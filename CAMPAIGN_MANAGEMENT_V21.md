# 원고 캠페인 관리 v2.1 (상세 보기 + 반응형 개편)

## 🎯 목표
- "상세 보기" 클릭 미작동 트리아지 (필수 체크 6가지)
- 반응형 레이아웃 개편 (가로 2열 → 모바일 세로 스택)
- 상세 패널 내용 구성 (역할·상태 수위 기준)
- 에러/빈 상태 UX 개선

---

## A. "상세 보기" 클릭 미작동 트리아지 (필수 체크 6가지)

### 📋 실행 단계

### 1. 이벤트 바인딩 확인

**확인 항목:**
- [ ] 행 전체 클릭 이벤트 바인딩 확인
- [ ] "상세 보기" 버튼 클릭 이벤트 바인딩 확인
- [ ] 단일 핸들러로 통합되어 있는지 확인
- [ ] 클릭 시 주문 `id`가 전달되는지 확인
- [ ] 데이터 속성(`data-order-id`) 또는 prop으로 `id` 전달 확인

**확인 방법:**
1. DevTools Elements 탭에서 행 요소 확인
2. "상세 보기" 버튼 요소 확인
3. 이벤트 리스너 확인 (Event Listeners 탭)
4. 클릭 시 콘솔 로그로 `id` 전달 확인

**예시 코드 구조:**
```javascript
// 단일 핸들러로 통합
const handleRowClick = (orderId) => {
  // 상세 패널 열기
};

// 행 클릭
<tr onClick={() => handleRowClick(order.id)}>

// 버튼 클릭
<button onClick={(e) => {
  e.stopPropagation();
  handleRowClick(order.id);
}}>
```

**확인 체크리스트:**
- [ ] 행 전체 클릭 이벤트 바인딩됨
- [ ] "상세 보기" 버튼 클릭 이벤트 바인딩됨
- [ ] 단일 핸들러로 통합됨
- [ ] 클릭 시 주문 `id` 전달됨
- [ ] 데이터 속성 또는 prop으로 `id` 전달 확인

---

### 2. 라우트/가드 확인

**확인 항목:**
- [ ] 대행사 상세 조회는 `GET /agency/orders/:id` 사용
- [ ] AGENCY 토큰/권한 가드 확인
- [ ] Network에서 403/401 응답 확인

**확인 방법:**
1. DevTools Network 탭 열기
2. "상세 보기" 클릭
3. 요청 확인:
   - [ ] `GET /agency/orders/:id` 요청 발생
   - [ ] Status Code: `200 OK` 또는 `403 Forbidden` 또는 `401 Unauthorized`
   - [ ] Request Headers: `Authorization: Bearer <JWT_TOKEN>`

**서버 검증:**
- [ ] JWT 토큰에서 `role=AGENCY` 확인
- [ ] `user.agencyId` 확인 (null 아님)
- [ ] 주문 소유권 확인 (`order.agencyId === user.agencyId`)

**확인 체크리스트:**
- [ ] `GET /agency/orders/:id` 요청 발생
- [ ] AGENCY 토큰/권한 가드 통과
- [ ] Network에서 403/401 응답 없음
- [ ] 주문 소유권 확인됨

---

### 3. 상세 패널 열림 조건 확인

**확인 항목:**
- [ ] fetch 성공(2xx) 시에만 오른쪽 패널 열기
- [ ] fetch 실패 시 top-center 토스트로 서버 메시지 노출

**확인 방법:**
1. "상세 보기" 클릭
2. Network에서 `GET /agency/orders/:id` 확인
3. 성공 케이스:
   - [ ] Status Code: `200 OK` 또는 `201 Created`
   - [ ] Response Body: 주문 데이터
   - [ ] 오른쪽 패널 열림
4. 실패 케이스:
   - [ ] Status Code: `400/401/403/404/500`
   - [ ] Response Body: `{ message: "..." }`
   - [ ] 패널 열리지 않음
   - [ ] top-center 토스트로 서버 메시지 표시

**예시 코드 구조:**
```javascript
const handleRowClick = async (orderId) => {
  try {
    const response = await fetch(`/agency/orders/${orderId}`);
    if (!response.ok) {
      const error = await response.json();
      toast.error(error.message); // top-center
      return;
    }
    const data = await response.json();
    setSelectedOrder(data);
    setIsPanelOpen(true); // 패널 열기
  } catch (error) {
    toast.error(error.message); // top-center
  }
};
```

**확인 체크리스트:**
- [ ] fetch 성공(2xx) 시에만 패널 열림
- [ ] fetch 실패 시 패널 열리지 않음
- [ ] 실패 시 top-center 토스트로 서버 메시지 표시

---

### 4. 역할별 비공개 규칙 확인

**확인 항목:**
- [ ] 상태가 `AGENCY_REVIEW`/`COMPLETE`가 아닐 땐 본문 미리보기 숨김
- [ ] "본문 비공개" 안내 문구가 패널 헤더에 표시되는지 확인

**확인 방법:**
1. 다양한 상태의 주문으로 테스트:
   - `SUBMITTED` 상태 주문
   - `GENERATING` 상태 주문
   - `AGENCY_REVIEW` 상태 주문
   - `COMPLETE` 상태 주문
2. 각 상태별로 패널 확인:
   - [ ] `AGENCY_REVIEW`: 본문 표시됨
   - [ ] `COMPLETE`: 본문 표시됨
   - [ ] 그 외 상태: 본문 숨김
   - [ ] 그 외 상태: "본문 비공개" 안내 문구 표시

**예시 UI 구조:**
```jsx
{order.status === 'AGENCY_REVIEW' || order.status === 'COMPLETE' ? (
  <div>{order.manuscript}</div>
) : (
  <div>
    <p>본문 비공개</p>
    <p>현재 상태에서는 본문을 확인할 수 없습니다.</p>
  </div>
)}
```

**확인 체크리스트:**
- [ ] `AGENCY_REVIEW` 상태: 본문 표시됨
- [ ] `COMPLETE` 상태: 본문 표시됨
- [ ] 그 외 상태: 본문 숨김
- [ ] 그 외 상태: "본문 비공개" 안내 문구 표시

---

### 5. 스켈레톤/에러 상태 확인

**확인 항목:**
- [ ] 패널 열기 → 스켈레톤 표시 → 성공 시 내용 교체
- [ ] 실패 시 스켈레톤 제거 + 오류 상태 컴포넌트 노출

**확인 방법:**
1. "상세 보기" 클릭
2. 즉시 확인:
   - [ ] 패널 열림
   - [ ] 스켈레톤 UI 표시
3. Network에서 응답 확인:
   - [ ] 성공(2xx): 스켈레톤 제거, 내용 표시
   - [ ] 실패(4xx/5xx): 스켈레톤 제거, 오류 상태 컴포넌트 표시

**예시 UI 구조:**
```jsx
const [loading, setLoading] = useState(false);
const [error, setError] = useState(null);
const [order, setOrder] = useState(null);

{loading && <Skeleton />}
{error && <ErrorState message={error.message} />}
{order && <OrderDetail order={order} />}
```

**확인 체크리스트:**
- [ ] 패널 열기 시 스켈레톤 표시
- [ ] 성공 시 스켈레톤 제거, 내용 표시
- [ ] 실패 시 스켈레톤 제거, 오류 상태 컴포넌트 표시

---

### 6. URL 동기화 확인 (선택 권장)

**확인 항목:**
- [ ] 행 클릭 시 `?id=123` 또는 `/agency/orders?id=123` 쿼리 반영
- [ ] 새로고침 복원: `?id=...`가 있으면 동일 상세 복원
- [ ] 뒤로가기/앞으로가기 시 패널 열림/닫힘 동기화

**확인 방법:**
1. 행 클릭
2. URL 확인:
   - [ ] `?id=123` 또는 `/agency/orders?id=123` 추가됨
3. 새로고침:
   - [ ] `?id=...`가 있으면 동일 상세 복원
   - [ ] 패널 자동 열림
4. 뒤로가기:
   - [ ] 패널 닫힘
   - [ ] URL에서 `?id=...` 제거
5. 앞으로가기:
   - [ ] 패널 열림
   - [ ] URL에 `?id=...` 추가

**예시 코드 구조:**
```javascript
// URL 동기화
const handleRowClick = (orderId) => {
  router.push(`/agency/orders?id=${orderId}`, { shallow: true });
  // 또는
  window.history.pushState({}, '', `?id=${orderId}`);
};

// 새로고침 복원
useEffect(() => {
  const id = new URLSearchParams(window.location.search).get('id');
  if (id) {
    loadOrderDetail(id);
  }
}, []);
```

**확인 체크리스트:**
- [ ] 행 클릭 시 URL 쿼리 반영
- [ ] 새로고침 시 동일 상세 복원
- [ ] 뒤로가기/앞으로가기 시 패널 열림/닫힘 동기화

---

### 검증문 (합격 기준)

**1. "상세 보기"/행 클릭 동작**
- [ ] "상세 보기"/행 클릭 → Network에 `GET /agency/orders/:id` 2xx
- [ ] 패널/모달 오픈
- [ ] 스켈레톤 표시 → 성공 시 내용 교체

**2. 실패 처리**
- [ ] 실패 시 서버 메시지가 top-center 토스트로 그대로 보임
- [ ] 스켈레톤 제거 + 오류 상태 컴포넌트 노출

**3. 역할별 비공개 규칙**
- [ ] `AGENCY_REVIEW`/`COMPLETE`만 본문 표시
- [ ] 그 외 상태는 비공개 안내 노출

**4. URL 동기화 (선택)**
- [ ] 새로고침해도 `?id=...`가 있으면 동일 상세가 복원됨
- [ ] 뒤로가기/앞으로가기 시 패널 열림/닫힘 동기화

---

## B. 반응형 레이아웃 개편 (가로 2열 → 모바일 세로 스택)

### 📋 실행 단계

### 1. 브레이크포인트 규칙 확인

**확인 항목:**
- [ ] `lg↑` (데스크톱): 좌 2/3 (리스트) + 우 1/3 (상세 패널 고정)
- [ ] `md↓` (태블릿/모바일): 단일 컬럼

**브레이크포인트 정의:**
- `lg`: 1024px 이상 (데스크톱)
- `md`: 768px ~ 1023px (태블릿)
- `sm`: 640px 이하 (모바일)

**확인 방법:**
1. 데스크톱 화면 (1920px × 1080px):
   - [ ] 좌측 2/3: 리스트 표시
   - [ ] 우측 1/3: 상세 패널 고정 표시
2. 태블릿 화면 (768px × 1024px):
   - [ ] 단일 컬럼: 세로 스택
   - [ ] 리스트 → 상세 드로어/페이지 전환
3. 모바일 화면 (375px × 667px):
   - [ ] 단일 컬럼: 세로 스택
   - [ ] 리스트 → 상세 드로어/페이지 전환

**예시 CSS 구조:**
```css
/* 데스크톱 */
@media (min-width: 1024px) {
  .container {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 1rem;
  }
}

/* 태블릿/모바일 */
@media (max-width: 1023px) {
  .container {
    display: flex;
    flex-direction: column;
  }
}
```

**확인 체크리스트:**
- [ ] `lg↑`: 좌 2/3 + 우 1/3 2열 레이아웃
- [ ] `md↓`: 단일 컬럼 세로 스택
- [ ] 브레이크포인트 전환 시 레이아웃 변경

---

### 2. 모바일 상세 표현 확인

**확인 항목:**
- [ ] 하단 시트(권장): 화면 85% 높이, 드래그로 닫기, 포커스 트랩/ESC 닫기
- [ ] 상세 페이지: `/agency/orders/[id]` 라우트 전환 (상단에 "뒤로가기")

**방법 1: 하단 시트 (권장)**

**확인 항목:**
- [ ] 화면 85% 높이
- [ ] 드래그로 닫기 (스와이프 다운)
- [ ] 포커스 트랩 (키보드 포커스가 시트 내부에 고정)
- [ ] ESC 키로 닫기
- [ ] 뒤로가기 버튼으로 닫기

**예시 UI 구조:**
```jsx
<BottomSheet
  isOpen={isOpen}
  onClose={handleClose}
  height="85vh"
  draggable
  focusTrap
  onEscape={handleClose}
>
  <OrderDetail order={order} />
</BottomSheet>
```

**방법 2: 상세 페이지**

**확인 항목:**
- [ ] `/agency/orders/[id]` 라우트로 전환
- [ ] 상단에 "뒤로가기" 버튼
- [ ] 뒤로가기 시 리스트로 복귀

**확인 방법:**
1. 리스트 행 클릭
2. 확인:
   - [ ] 하단 시트 열림 또는 상세 페이지로 전환
   - [ ] 뒤로가기 시 리스트로 복귀

**확인 체크리스트:**
- [ ] 모바일 상세 표현: 하단 시트 또는 상세 페이지
- [ ] 하단 시트: 85% 높이, 드래그, 포커스 트랩, ESC 닫기
- [ ] 상세 페이지: 라우트 전환, 뒤로가기 버튼

---

### 3. 상단 툴바 정리 (모바일 가독성)

**확인 항목:**
- [ ] 상단 카드 (작성 중/1차 검수/금일 완료) → 요약 줄 카드 3개
- [ ] 바로 아래 탭 (작성 중/1차/금일 완료), 검색창 (업체/키워드), 정렬 드롭다운
- [ ] 툴바는 sticky로 고정 (스크롤해도 탭/검색 유지)

**확인 방법:**
1. 모바일 화면 (375px × 667px):
   - [ ] 상단 카드: 요약 줄 카드 3개 표시
   - [ ] 탭: 작성 중/1차/금일 완료 표시
   - [ ] 검색창: 업체/키워드 검색
   - [ ] 정렬 드롭다운 표시
2. 스크롤 테스트:
   - [ ] 스크롤해도 툴바 고정 (sticky)
   - [ ] 탭/검색 유지

**예시 UI 구조:**
```jsx
<div className="sticky top-0 bg-white z-10">
  {/* 요약 카드 3개 */}
  <div className="grid grid-cols-3 gap-2">
    <Card>작성 중: 5</Card>
    <Card>1차 검수: 2</Card>
    <Card>금일 완료: 1</Card>
  </div>
  
  {/* 탭 */}
  <Tabs>
    <Tab>작성 중</Tab>
    <Tab>1차 검수</Tab>
    <Tab>금일 완료</Tab>
  </Tabs>
  
  {/* 검색/정렬 */}
  <div>
    <SearchInput placeholder="업체/키워드" />
    <SortDropdown />
  </div>
</div>
```

**확인 체크리스트:**
- [ ] 상단 카드: 요약 줄 카드 3개
- [ ] 탭: 작성 중/1차/금일 완료
- [ ] 검색창: 업체/키워드 검색
- [ ] 정렬 드롭다운
- [ ] 툴바 sticky 고정

---

### 4. 리스트 가독성 (모바일 카드형)

**확인 항목:**
- [ ] `md↓`에선 테이블 대신 카드형:
  - 1행: 상태 뱃지 + 최근 갱신 시간
  - 2행: 업체명 / 키워드 요약
  - 3행: 사진 수, 그룹ID (있으면)
  - 우측 끝: 상세 아이콘 버튼

**확인 방법:**
1. 모바일 화면 (375px × 667px):
   - [ ] 테이블 대신 카드형 표시
   - [ ] 1행: 상태 뱃지 + 최근 갱신 시간
   - [ ] 2행: 업체명 / 키워드 요약
   - [ ] 3행: 사진 수, 그룹ID (있으면)
   - [ ] 우측 끝: 상세 아이콘 버튼

**예시 UI 구조:**
```jsx
<div className="md:hidden">
  {orders.map(order => (
    <Card key={order.id}>
      <div className="flex justify-between items-center">
        <Badge>{order.status}</Badge>
        <span>{formatDate(order.updatedAt)}</span>
      </div>
      <div>
        <h3>{order.place.name}</h3>
        <p>{order.guide.searchKeywords.join(', ')}</p>
      </div>
      <div>
        <span>사진 {order.photos.length}장</span>
        {order.groupId && <span>그룹 {order.groupId}</span>}
      </div>
      <button onClick={() => handleRowClick(order.id)}>
        <Icon name="chevron-right" />
      </button>
    </Card>
  ))}
</div>
```

**확인 체크리스트:**
- [ ] `md↓`: 테이블 대신 카드형
- [ ] 1행: 상태 뱃지 + 최근 갱신 시간
- [ ] 2행: 업체명 / 키워드 요약
- [ ] 3행: 사진 수, 그룹ID (있으면)
- [ ] 우측 끝: 상세 아이콘 버튼

---

### 5. 접근성/키보드 확인

**확인 항목:**
- [ ] 행 포커스 가능 (Enter=열기, Arrow ↑↓=행 이동)
- [ ] 드로어/모달에 포커스 트랩 + ESC 닫기
- [ ] SR (스크린리더)을 위한 상태·라벨 텍스트 제공

**확인 방법:**
1. 키보드 네비게이션 테스트:
   - [ ] Tab 키로 행 포커스 이동
   - [ ] Enter 키로 상세 열기
   - [ ] Arrow ↑↓ 키로 행 이동
2. 드로어/모달 테스트:
   - [ ] 포커스 트랩: Tab 키로 드로어 내부 포커스 이동
   - [ ] ESC 키로 닫기
3. 스크린리더 테스트:
   - [ ] 상태 뱃지: "작성 중" 등 텍스트 제공
   - [ ] 라벨 텍스트: "상세 보기" 등 제공

**예시 코드 구조:**
```jsx
<tr
  tabIndex={0}
  onKeyDown={(e) => {
    if (e.key === 'Enter') {
      handleRowClick(order.id);
    } else if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
      // 행 이동
    }
  }}
>
  <td>
    <Badge aria-label={`상태: ${order.status}`}>
      {order.status}
    </Badge>
  </td>
</tr>
```

**확인 체크리스트:**
- [ ] 행 포커스 가능 (Enter=열기, Arrow ↑↓=행 이동)
- [ ] 드로어/모달 포커스 트랩 + ESC 닫기
- [ ] SR을 위한 상태·라벨 텍스트 제공

---

### 6. 성능/대용량 대응 확인

**확인 항목:**
- [ ] 1000건↑ 대비 가상 스크롤 도입 검토
- [ ] 페이지네이션 유지 (20/페이지) + "더 보기" 버튼 (모바일)

**확인 방법:**
1. 대용량 데이터 테스트:
   - [ ] 1000건 이상 데이터 로드
   - [ ] 가상 스크롤 동작 확인
2. 페이지네이션 테스트:
   - [ ] 20건/페이지 유지
   - [ ] "더 보기" 버튼 클릭 시 다음 페이지 로드

**확인 체크리스트:**
- [ ] 1000건↑ 대비 가상 스크롤 도입 검토
- [ ] 페이지네이션 유지 (20/페이지)
- [ ] "더 보기" 버튼 (모바일)

---

### 검증문 (합격 기준)

**1. 데스크톱 레이아웃**
- [ ] `lg↑`: 좌/우 2열 동작
- [ ] 패널이 고정 표시

**2. 모바일/태블릿 레이아웃**
- [ ] `md↓`: 상단 카드/탭/검색 고정
- [ ] 리스트 → 상세 드로어 (또는 상세 페이지) 전환 자연

**3. 네비게이션**
- [ ] 뒤로가기/앞으로가기 시 리스트↔상세 전환이 매끄럽고 포커스가 복원됨

---

## C. 상세 패널 내용 구성 (역할·상태 수위 기준)

### 📋 실행 단계

### 1. 헤더 구성 확인

**확인 항목:**
- [ ] 업체명
- [ ] 상태 뱃지
- [ ] 최근 갱신
- [ ] 사진 수

**확인 방법:**
1. 상세 패널 열기
2. 헤더 확인:
   - [ ] 업체명 표시
   - [ ] 상태 뱃지 표시
   - [ ] 최근 갱신 시간 표시
   - [ ] 사진 수 표시

**확인 체크리스트:**
- [ ] 업체명
- [ ] 상태 뱃지
- [ ] 최근 갱신
- [ ] 사진 수

---

### 2. 본문 구성 확인

**확인 항목:**
- [ ] 요약 (키워드·비고)
- [ ] 원고 본문 (단, `AGENCY_REVIEW`/`COMPLETE`에서만 렌더)

**확인 방법:**
1. 다양한 상태의 주문으로 테스트:
   - `SUBMITTED` 상태: 요약만 표시, 본문 숨김
   - `AGENCY_REVIEW` 상태: 요약 + 본문 표시
   - `COMPLETE` 상태: 요약 + 본문 표시
2. 요약 확인:
   - [ ] 키워드 표시
   - [ ] 비고 표시
3. 본문 확인:
   - [ ] `AGENCY_REVIEW`: 본문 표시
   - [ ] `COMPLETE`: 본문 표시
   - [ ] 그 외 상태: 본문 숨김

**확인 체크리스트:**
- [ ] 요약 (키워드·비고)
- [ ] 원고 본문 (`AGENCY_REVIEW`/`COMPLETE`에서만 렌더)

---

### 3. 검수 액션 확인

**확인 항목:**
- [ ] 통과 (Approve) → `COMPLETE` + 성공 토스트
- [ ] 반려 (Reject) → 사유 입력 필수 → 자동 재생성 루프 + "작성 중" 버킷으로 즉시 이동

**확인 방법:**
1. `AGENCY_REVIEW` 상태 주문 열기
2. 통과 (Approve) 테스트:
   - [ ] "통과" 버튼 클릭
   - [ ] 상태: `COMPLETE`로 변경
   - [ ] top-center 성공 토스트 표시
   - [ ] 리스트/카운터/상단 카드 즉시 반영
3. 반려 (Reject) 테스트:
   - [ ] "반려" 버튼 클릭
   - [ ] 사유 입력 필수 (모달/입력 필드)
   - [ ] 사유 제출 시 상태: `REGEN_QUEUED`로 변경
   - [ ] 자동 재생성 루프 시작
   - [ ] "작성 중" 버킷으로 즉시 이동
   - [ ] 리스트/카운터/상단 카드 즉시 반영

**확인 체크리스트:**
- [ ] 통과 (Approve) → `COMPLETE` + 성공 토스트
- [ ] 반려 (Reject) → 사유 입력 필수 → 자동 재생성 루프

---

### 4. 꼬리 구성 확인 (선택)

**확인 항목:**
- [ ] 활동 로그 (선택)
- [ ] 다운로드/복사 (선택)

**확인 방법:**
1. 상세 패널 하단 확인:
   - [ ] 활동 로그 표시 (선택)
   - [ ] 다운로드 버튼 (선택)
   - [ ] 복사 버튼 (선택)

**확인 체크리스트:**
- [ ] 활동 로그 (선택)
- [ ] 다운로드/복사 (선택)

---

### 검증문 (합격 기준)

**1. 상태 변경 반영**
- [ ] 상태가 바뀌면 (통과/반려) 리스트/카운터/상단 카드가 즉시 반영

**2. 본문 비공개 규칙**
- [ ] 본문 비공개 상태에서 본문이 절대 노출되지 않음

---

## D. 에러/빈 상태 UX

### 📋 실행 단계

### 1. 에러 상태 확인

**확인 항목:**
- [ ] 서버 메시지 원문을 top-center 토스트
- [ ] "다시 시도" 버튼 제공

**확인 방법:**
1. 네트워크 오류 시뮬레이션:
   - [ ] `GET /agency/orders/:id` 실패 (500 에러)
   - [ ] 서버 메시지 원문이 top-center 토스트로 표시
   - [ ] "다시 시도" 버튼 표시
   - [ ] "다시 시도" 버튼 클릭 시 재시도

**확인 체크리스트:**
- [ ] 서버 메시지 원문 top-center 토스트
- [ ] "다시 시도" 버튼 제공

---

### 2. 빈 리스트 상태 확인

**확인 항목:**
- [ ] "현재 조건에 해당하는 원고가 없습니다."
- [ ] "모든 원고 보기" 버튼

**확인 방법:**
1. 필터 조건으로 빈 리스트 생성
2. 빈 리스트 확인:
   - [ ] "현재 조건에 해당하는 원고가 없습니다." 메시지 표시
   - [ ] "모든 원고 보기" 버튼 표시
   - [ ] "모든 원고 보기" 버튼 클릭 시 필터 초기화

**확인 체크리스트:**
- [ ] 빈 리스트 메시지 표시
- [ ] "모든 원고 보기" 버튼

---

## E. 검증 체크리스트 (최종)

### ✅ 종합 검증

**1. "상세 보기" 클릭 동작**
- [ ] "상세 보기" 클릭 → `GET /agency/orders/:id` 2xx
- [ ] 패널/드로어 오픈

**2. 반응형 레이아웃**
- [ ] 모바일/태블릿에서 세로 스택 동작
- [ ] 툴바 sticky, 카드형 리스트

**3. 상태 변경 반영**
- [ ] 통과/반려 시 리스트/카운터/상단 카드 실시간 반영

**4. 본문 비공개 규칙**
- [ ] 본문 비공개 상태에서 본문 렌더 X

**5. 알림 표시**
- [ ] 모든 알림은 top-center로 서버 메시지 원문 표출

---

## 📊 트리아지 리포트 템플릿

### "상세 보기" 클릭 미작동 리포트 작성 시:

```
## "상세 보기" 클릭 미작동 리포트

### 1. 이벤트 바인딩
- 행 전체 클릭: [ ] 바인딩됨 [ ] 바인딩 안 됨
- "상세 보기" 버튼 클릭: [ ] 바인딩됨 [ ] 바인딩 안 됨
- 주문 id 전달: [ ] 전달됨 [ ] 전달 안 됨

### 2. 라우트/가드
- GET /agency/orders/:id: [ ] 요청 발생 [ ] 요청 안 됨
- Status Code: [ ] 200 [ ] 403/401 [ ] 500
- AGENCY 토큰/권한: [ ] 통과 [ ] 실패

### 3. 상세 패널 열림 조건
- fetch 성공(2xx): [ ] 패널 열림 [ ] 패널 안 열림
- fetch 실패: [ ] 토스트 표시 [ ] 토스트 안 표시

### 4. 역할별 비공개 규칙
- AGENCY_REVIEW/COMPLETE: [ ] 본문 표시 [ ] 본문 숨김
- 그 외 상태: [ ] 본문 숨김 [ ] 본문 표시

### 5. 스켈레톤/에러 상태
- 패널 열기 → 스켈레톤: [ ] 표시 [ ] 표시 안 됨
- 성공 시 내용 교체: [ ] 교체됨 [ ] 교체 안 됨
- 실패 시 오류 상태: [ ] 표시 [ ] 표시 안 됨

### 6. URL 동기화
- 행 클릭 시 URL 쿼리: [ ] 반영됨 [ ] 반영 안 됨
- 새로고침 복원: [ ] 복원됨 [ ] 복원 안 됨
- 뒤로가기/앞으로가기: [ ] 동기화됨 [ ] 동기화 안 됨

### 7. 원인 추정
1순위: [ ] 이벤트 바인딩 문제
2순위: [ ] 라우트/가드 문제
3순위: [ ] 상세 패널 열림 조건 문제

### 8. 조치
- [ ] 이벤트 바인딩 확인/수정
- [ ] 라우트/가드 확인/수정
- [ ] 상세 패널 열림 조건 확인/수정
- [ ] 스켈레톤/에러 상태 구현
- [ ] URL 동기화 구현 (선택)
```






