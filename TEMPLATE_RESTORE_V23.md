# "최근 템플릿 가져오기" 복구 v2.3 (대행사 원고 접수)

## 🎯 목표
- 저장 시점 명확화 (템플릿 스냅샷)
- 식별 키/중복 규칙 정의
- 읽기(불러오기) 트리거 규칙
- API 계약 정의
- 적용(덮어쓰기) 동작 규칙
- 권한/보안 확인
- 실패 원인 트리아지
- UI 개선 (권장)

---

## A. 저장 시점 (템플릿 스냅샷)

### 📋 실행 단계

### 1. 저장 트리거 확인

**확인 항목:**
- [ ] 임시 저장(DRAFT) 성공 직후 저장
- [ ] 최종 접수(SUBMITTED) 성공 직후 저장

**확인 방법:**
1. 임시 저장(DRAFT) 테스트:
   - [ ] 주문 폼 작성
   - [ ] "[임시 저장]" 버튼 클릭
   - [ ] DevTools Network에서 `POST /agency/order-templates` 호출 확인
   - [ ] 저장 성공 직후 호출 확인
2. 최종 접수(SUBMITTED) 테스트:
   - [ ] 주문 폼 작성
   - [ ] "[접수]" 버튼 클릭
   - [ ] DevTools Network에서 `POST /agency/order-templates` 호출 확인
   - [ ] 저장 성공 직후 호출 확인

**확인 체크리스트:**
- [ ] 임시 저장(DRAFT) 성공 직후 저장 트리거
- [ ] 최종 접수(SUBMITTED) 성공 직후 저장 트리거

---

### 2. 저장 범위 확인

**확인 항목:**
- [ ] 텍스트/체크 항목만 저장:
  - [ ] `searchKeywords`
  - [ ] `includeText` (원고에 들어갈 내용)
  - [ ] `requiredKeywords`
  - [ ] `emphasizeKeywords`
  - [ ] `flags` (link/map)
  - [ ] `hashtags` (≤5)
  - [ ] `notes`
- [ ] 사진은 제외 (항상 새 업로드 원칙)

**확인 방법:**
1. DevTools Network에서 `POST /agency/order-templates` 요청 확인
2. Request Payload 확인:
   ```json
   {
     "placeName": "...",
     "snapshot": {
       "searchKeywords": ["..."],
       "includeText": "...",
       "requiredKeywords": ["..."],
       "emphasizeKeywords": ["..."],
       "flags": { "link": true, "map": false },
       "hashtags": ["..."],
       "notes": "..."
       // photos는 없어야 함
     }
   }
   ```

**확인 체크리스트:**
- [ ] 텍스트/체크 항목만 저장
- [ ] 사진은 제외

---

## B. 식별 키/중복 규칙

### 📋 실행 단계

### 1. 식별 키 확인

**확인 항목:**
- [ ] 키: `(agencyId, placeNameNormalized)`
- [ ] 정규화 규칙:
  - [ ] 앞뒤 공백 제거
  - [ ] 연속 공백 1개로 축약
  - [ ] 대소문자 무시

**확인 방법:**
1. 정규화 규칙 테스트:
   - [ ] 입력: `"  테스트  업체  "` → 정규화: `"테스트 업체"`
   - [ ] 입력: `"테스트업체"` → 정규화: `"테스트업체"`
   - [ ] 입력: `"Test 업체"` → 정규화: `"test 업체"` (대소문자 무시)
2. 서버/클라이언트 정규화 규칙 일치 확인:
   - [ ] 프론트엔드 정규화 함수 확인
   - [ ] 백엔드 정규화 함수 확인
   - [ ] 두 규칙이 동일한지 확인

**확인 체크리스트:**
- [ ] 키: `(agencyId, placeNameNormalized)`
- [ ] 정규화 규칙: 앞뒤 공백 제거, 연속 공백 1개로 축약, 대소문자 무시
- [ ] 프론트엔드/백엔드 정규화 규칙 일치

---

### 2. 중복 규칙 확인

**확인 항목:**
- [ ] 템플릿은 최신순 N=5개까지만 유지
- [ ] 넘으면 가장 오래된 것 삭제

**확인 방법:**
1. 템플릿 저장 테스트:
   - [ ] 동일 업체명으로 6번 이상 저장
   - [ ] DB에서 템플릿 개수 확인 (최대 5개)
   - [ ] 가장 오래된 템플릿 삭제 확인
2. 템플릿 읽기 테스트:
   - [ ] `GET /agency/order-templates?place=<name>` 호출
   - [ ] Response Body에서 최대 5개 반환 확인
   - [ ] 최신순 정렬 확인

**확인 체크리스트:**
- [ ] 템플릿은 최신순 N=5개까지만 유지
- [ ] 넘으면 가장 오래된 것 삭제

---

## C. 읽기(불러오기) 트리거

### 📋 실행 단계

### 1. 드롭다운 갱신 규칙 확인

**확인 항목:**
- [ ] `placeName` 입력이 2자 이상이 되면 디바운스 400ms 후 요청
- [ ] 입력이 비면 목록 숨김
- [ ] 요청 중에는 로딩 인디케이터 표시

**확인 방법:**
1. `placeName` 입력 테스트:
   - [ ] 1자 입력 → 요청 없음
   - [ ] 2자 이상 입력 → 디바운스 400ms 후 요청
   - [ ] 입력 삭제 (빈 문자열) → 목록 숨김
2. 로딩 인디케이터 테스트:
   - [ ] 요청 중: 로딩 인디케이터 표시
   - [ ] 요청 완료: 로딩 인디케이터 제거

**확인 체크리스트:**
- [ ] `placeName` 입력이 2자 이상이 되면 디바운스 400ms 후 요청
- [ ] 입력이 비면 목록 숨김
- [ ] 요청 중에는 로딩 인디케이터 표시

---

### 2. 디바운스 적용 확인

**확인 항목:**
- [ ] 디바운스 400ms 적용
- [ ] 연속 입력 시 마지막 입력 후 400ms 대기 후 요청

**확인 방법:**
1. 연속 입력 테스트:
   - [ ] "테" 입력 → 400ms 대기
   - [ ] "테스" 입력 → 이전 타이머 취소, 400ms 대기
   - [ ] "테스트" 입력 → 이전 타이머 취소, 400ms 대기
   - [ ] 400ms 후 요청 발생
2. DevTools Network에서 요청 횟수 확인:
   - [ ] 연속 입력 시 요청 횟수 최소화 (디바운스 적용)

**확인 체크리스트:**
- [ ] 디바운스 400ms 적용
- [ ] 연속 입력 시 마지막 입력 후 400ms 대기 후 요청

---

## D. API 계약 (엔드포인트 수준)

### 📋 실행 단계

### 1. GET /agency/order-templates 확인

**확인 항목:**
- [ ] 엔드포인트: `GET /agency/order-templates?place=<rawPlaceName>`
- [ ] 서버는 `agencyId` 스코프에서 `placeNameNormalized`로 매칭
- [ ] 최신 N개 반환

**확인 방법:**
1. DevTools Network에서 요청 확인:
   - [ ] `GET /agency/order-templates?place=테스트업체`
   - [ ] Request Headers: `Authorization: Bearer <JWT_TOKEN>`
2. Response Body 확인:
   - [ ] `agencyId` 스코프로 필터링됨
   - [ ] `placeNameNormalized`로 매칭됨
   - [ ] 최대 5개 반환
   - [ ] 최신순 정렬

**확인 체크리스트:**
- [ ] `GET /agency/order-templates?place=<rawPlaceName>`
- [ ] 서버는 `agencyId` 스코프에서 `placeNameNormalized`로 매칭
- [ ] 최신 N개 반환

---

### 2. POST /agency/order-templates 확인

**확인 항목:**
- [ ] 엔드포인트: `POST /agency/order-templates`
- [ ] 본문: `{ placeName, snapshot }`
- [ ] 서버에서 `agencyId` 바인딩
- [ ] 호출은 A의 두 시점에서만 수행

**확인 방법:**
1. DevTools Network에서 요청 확인:
   - [ ] 임시 저장(DRAFT) 성공 직후 호출
   - [ ] 최종 접수(SUBMITTED) 성공 직후 호출
2. Request Payload 확인:
   ```json
   {
     "placeName": "테스트업체",
     "snapshot": {
       "searchKeywords": ["..."],
       "includeText": "...",
       "requiredKeywords": ["..."],
       "emphasizeKeywords": ["..."],
       "flags": { "link": true, "map": false },
       "hashtags": ["..."],
       "notes": "..."
     }
   }
   ```
3. 서버 응답 확인:
   - [ ] `agencyId` 자동 바인딩 (JWT에서 추출)
   - [ ] 쿼리 파라미터의 `agencyId` 무시 (스푸핑 차단)

**확인 체크리스트:**
- [ ] `POST /agency/order-templates`
- [ ] 본문: `{ placeName, snapshot }`
- [ ] 서버에서 `agencyId` 바인딩
- [ ] 호출은 A의 두 시점에서만 수행

---

## E. 적용(덮어쓰기) 동작

### 📋 실행 단계

### 1. 덮어쓰기 동작 확인

**확인 항목:**
- [ ] 드롭다운에서 항목 선택 시, 현재 폼의 텍스트/체크 필드만 완전 덮어쓰기
- [ ] 사진 카운터/업로드 상태는 변경하지 않음

**확인 방법:**
1. 템플릿 선택 테스트:
   - [ ] 주문 폼에 기존 데이터 입력
   - [ ] 사진 15~20장 업로드
   - [ ] 템플릿 드롭다운에서 항목 선택
   - [ ] 텍스트/체크 필드만 덮어쓰기 확인
   - [ ] 사진 카운터/업로드 상태 유지 확인
2. 필드 확인:
   - [ ] `searchKeywords`: 덮어쓰기
   - [ ] `includeText`: 덮어쓰기
   - [ ] `requiredKeywords`: 덮어쓰기
   - [ ] `emphasizeKeywords`: 덮어쓰기
   - [ ] `flags`: 덮어쓰기
   - [ ] `hashtags`: 덮어쓰기
   - [ ] `notes`: 덮어쓰기
   - [ ] 사진: 유지

**확인 체크리스트:**
- [ ] 텍스트/체크 필드만 완전 덮어쓰기
- [ ] 사진 카운터/업로드 상태 변경하지 않음

---

### 2. 즉석 검증 확인

**확인 항목:**
- [ ] 적용 즉시, 해시태그 5개 초과/필수 항목 누락 여부를 즉석 검증
- [ ] 문제가 있으면 top-center 토스트로 안내

**확인 방법:**
1. 해시태그 5개 초과 테스트:
   - [ ] 템플릿에 해시태그 6개 이상 포함
   - [ ] 템플릿 선택
   - [ ] 즉석 검증: 해시태그 5개 초과 감지
   - [ ] top-center 토스트: "해시태그는 최대 5개까지 가능합니다"
2. 필수 항목 누락 테스트:
   - [ ] 템플릿에 `searchKeywords` 없음
   - [ ] 템플릿 선택
   - [ ] 즉석 검증: 필수 항목 누락 감지
   - [ ] top-center 토스트: "검색 키워드는 필수입니다"

**확인 체크리스트:**
- [ ] 적용 즉시, 해시태그 5개 초과/필수 항목 누락 여부 즉석 검증
- [ ] 문제가 있으면 top-center 토스트로 안내

---

## F. 권한/보안

### 📋 실행 단계

### 1. 역할 확인

**확인 항목:**
- [ ] 템플릿 API는 AGENCY 역할만 접근
- [ ] 서버는 항상 JWT에서 `agencyId`를 바인딩
- [ ] 쿼리 파라미터의 `agencyId`는 무시 (스푸핑 차단)

**확인 방법:**
1. 권한 테스트:
   - [ ] AGENCY 역할로 로그인 → 템플릿 API 접근 가능
   - [ ] ADMIN 역할로 로그인 → 템플릿 API 접근 불가 (403)
2. 스푸핑 차단 테스트:
   - [ ] Request Payload에 `agencyId` 포함
   - [ ] 서버는 JWT에서 `agencyId` 추출
   - [ ] Request Payload의 `agencyId` 무시

**확인 체크리스트:**
- [ ] 템플릿 API는 AGENCY 역할만 접근
- [ ] 서버는 항상 JWT에서 `agencyId`를 바인딩
- [ ] 쿼리 파라미터의 `agencyId`는 무시 (스푸핑 차단)

---

## G. 실패 원인 트리아지 (이번 '안 됨' 재현 시 필수 확인)

### 📋 실행 단계

### 1. placeName 정규화 미스매치

**증상:**
- [ ] 프론트엔드와 백엔드의 정규화 규칙이 동일하지 않음
- [ ] 공백/대소문자 처리 불일치

**확인:**
1. 정규화 규칙 비교:
   - [ ] 프론트엔드 정규화 함수 확인
   - [ ] 백엔드 정규화 함수 확인
   - [ ] 두 규칙이 동일한지 확인
2. 테스트 케이스:
   - [ ] 입력: `"  테스트  업체  "` → 프론트: `"테스트 업체"`, 백엔드: `"테스트 업체"`
   - [ ] 입력: `"Test 업체"` → 프론트: `"test 업체"`, 백엔드: `"test 업체"`

**확인 체크리스트:**
- [ ] 프론트엔드/백엔드 정규화 규칙 일치
- [ ] 공백/대소문자 처리 일치

---

### 2. 저장 시점 누락

**증상:**
- [ ] DRAFT/접수 성공 후 `POST /agency/order-templates`가 실제로 호출되지 않음

**확인:**
1. Network 캡처:
   - [ ] 임시 저장(DRAFT) 성공 직후 `POST /agency/order-templates` 호출 확인
   - [ ] 최종 접수(SUBMITTED) 성공 직후 `POST /agency/order-templates` 호출 확인
2. 호출 순서 확인:
   - [ ] `POST /orders` 성공 (2xx)
   - [ ] `POST /agency/order-templates` 호출 (즉시 또는 약간의 지연)

**확인 체크리스트:**
- [ ] DRAFT 성공 직후 `POST /agency/order-templates` 호출
- [ ] SUBMITTED 성공 직후 `POST /agency/order-templates` 호출

---

### 3. 스코프 누락

**증상:**
- [ ] 백엔드가 `agencyId` 없이 전역 검색하고 있음
- [ ] 반드시 소속 범위만 검색해야 함

**확인:**
1. 서버 코드 확인:
   - [ ] `GET /agency/order-templates`에서 `agencyId` 필터링 확인
   - [ ] 전역 검색 없음 확인
2. 테스트:
   - [ ] Agency A로 로그인 → Agency A의 템플릿만 반환
   - [ ] Agency B로 로그인 → Agency B의 템플릿만 반환

**확인 체크리스트:**
- [ ] 백엔드가 `agencyId` 스코프로 필터링
- [ ] 전역 검색 없음

---

### 4. 디바운스 미적용/과도

**증상:**
- [ ] `placeName` 변경 후 400ms 내 호출됨
- [ ] 너무 짧아 연속 호출됨

**확인:**
1. 디바운스 적용 확인:
   - [ ] `placeName` 변경 후 400ms 대기 후 요청
   - [ ] 연속 입력 시 마지막 입력 후 400ms 대기
2. DevTools Network에서 요청 횟수 확인:
   - [ ] 연속 입력 시 요청 횟수 최소화

**확인 체크리스트:**
- [ ] 디바운스 400ms 적용
- [ ] 연속 입력 시 요청 횟수 최소화

---

### 5. 빈 목록 처리

**증상:**
- [ ] 서버가 200+빈 배열을 보낼 때 드롭다운이 "템플릿 없음" 안내로 바뀌지 않음

**확인:**
1. 빈 목록 테스트:
   - [ ] 존재하지 않는 업체명 입력
   - [ ] 서버 응답: `200 OK`, `[]` (빈 배열)
   - [ ] 드롭다운: "템플릿 없음" 안내 표시
2. UI 확인:
   - [ ] 빈 목록일 때 드롭다운 비활성화 또는 "템플릿 없음" 메시지 표시

**확인 체크리스트:**
- [ ] 서버가 200+빈 배열을 보낼 때 드롭다운이 "템플릿 없음" 안내로 바뀜

---

## H. UI 사소 개선 (권장)

### 📋 실행 단계

### 1. 드롭다운 헤더

**확인 항목:**
- [ ] 드롭다운 헤더: "최근 템플릿(업체명 일치, 최신순)"

**확인 방법:**
1. UI 확인:
   - [ ] 드롭다운 헤더 텍스트 확인
   - [ ] "최근 템플릿(업체명 일치, 최신순)" 표시

**확인 체크리스트:**
- [ ] 드롭다운 헤더: "최근 템플릿(업체명 일치, 최신순)"

---

### 2. 항목 서브텍스트

**확인 항목:**
- [ ] 최근 저장 시각 표시
- [ ] 키워드 요약 1~2개 표시

**확인 방법:**
1. UI 확인:
   - [ ] 템플릿 항목에 최근 저장 시각 표시
   - [ ] 템플릿 항목에 키워드 요약 1~2개 표시

**확인 체크리스트:**
- [ ] 최근 저장 시각 표시
- [ ] 키워드 요약 1~2개 표시

---

### 3. 비정상 값 표시

**확인 항목:**
- [ ] 비정상 값(해시태그 5개 초과 등) 포함 템플릿에는 주의 아이콘 표시
- [ ] 적용 시 자동 교정/경고 토스트

**확인 방법:**
1. 비정상 값 테스트:
   - [ ] 해시태그 6개 이상 포함 템플릿
   - [ ] 주의 아이콘 표시
   - [ ] 템플릿 선택 시 자동 교정 (5개로 제한) 또는 경고 토스트

**확인 체크리스트:**
- [ ] 비정상 값 포함 템플릿에는 주의 아이콘 표시
- [ ] 적용 시 자동 교정/경고 토스트

---

## 📊 종합 검증 체크리스트

### ✅ 저장 시점
1. [ ] 임시 저장(DRAFT) 성공 직후 저장 트리거
2. [ ] 최종 접수(SUBMITTED) 성공 직후 저장 트리거
3. [ ] 텍스트/체크 항목만 저장 (사진 제외)

### ✅ 식별 키/중복 규칙
1. [ ] 키: `(agencyId, placeNameNormalized)`
2. [ ] 정규화 규칙: 앞뒤 공백 제거, 연속 공백 1개로 축약, 대소문자 무시
3. [ ] 템플릿은 최신순 N=5개까지만 유지

### ✅ 읽기(불러오기) 트리거
1. [ ] `placeName` 입력이 2자 이상이 되면 디바운스 400ms 후 요청
2. [ ] 입력이 비면 목록 숨김
3. [ ] 요청 중에는 로딩 인디케이터 표시

### ✅ API 계약
1. [ ] `GET /agency/order-templates?place=<rawPlaceName>`
2. [ ] 서버는 `agencyId` 스코프에서 `placeNameNormalized`로 매칭
3. [ ] `POST /agency/order-templates` 본문: `{ placeName, snapshot }`
4. [ ] 서버에서 `agencyId` 바인딩

### ✅ 적용(덮어쓰기) 동작
1. [ ] 텍스트/체크 필드만 완전 덮어쓰기
2. [ ] 사진 카운터/업로드 상태 변경하지 않음
3. [ ] 적용 즉시, 해시태그 5개 초과/필수 항목 누락 여부 즉석 검증
4. [ ] 문제가 있으면 top-center 토스트로 안내

### ✅ 권한/보안
1. [ ] 템플릿 API는 AGENCY 역할만 접근
2. [ ] 서버는 항상 JWT에서 `agencyId`를 바인딩
3. [ ] 쿼리 파라미터의 `agencyId`는 무시 (스푸핑 차단)

### ✅ 실패 원인 트리아지
1. [ ] placeName 정규화 미스매치 확인
2. [ ] 저장 시점 누락 확인 (Network 캡처)
3. [ ] 스코프 누락 확인
4. [ ] 디바운스 미적용/과도 확인
5. [ ] 빈 목록 처리 확인

### ✅ UI 개선 (권장)
1. [ ] 드롭다운 헤더: "최근 템플릿(업체명 일치, 최신순)"
2. [ ] 항목 서브텍스트: 최근 저장 시각, 키워드 요약 1~2개
3. [ ] 비정상 값 포함 템플릿에는 주의 아이콘 표시

---

## 🔧 트리아지 리포트 템플릿

### "최근 템플릿 가져오기" 복구 실패 리포트 작성 시:

```
## "최근 템플릿 가져오기" 복구 실패 리포트

### 1. 저장 시점
- 임시 저장(DRAFT) 성공 직후: [ ] 호출됨 [ ] 호출 안 됨
- 최종 접수(SUBMITTED) 성공 직후: [ ] 호출됨 [ ] 호출 안 됨
- Network 캡처: [ ] POST /agency/order-templates 호출 확인

### 2. 식별 키/중복 규칙
- 키: [ ] (agencyId, placeNameNormalized)
- 정규화 규칙: [ ] 프론트/백엔드 일치 [ ] 불일치
- 템플릿 개수: [ ] 최대 5개 유지 [ ] 초과

### 3. 읽기(불러오기) 트리거
- placeName 입력 2자 이상: [ ] 디바운스 400ms 적용 [ ] 미적용
- 입력이 비면: [ ] 목록 숨김 [ ] 목록 유지
- 요청 중: [ ] 로딩 인디케이터 표시 [ ] 표시 안 됨

### 4. API 계약
- GET /agency/order-templates: [ ] 호출됨 [ ] 호출 안 됨
- 서버 agencyId 스코프: [ ] 필터링됨 [ ] 전역 검색
- POST /agency/order-templates: [ ] 호출됨 [ ] 호출 안 됨

### 5. 적용(덮어쓰기) 동작
- 텍스트/체크 필드 덮어쓰기: [ ] 작동 [ ] 작동 안 함
- 사진 카운터/업로드 상태: [ ] 유지 [ ] 변경됨
- 즉석 검증: [ ] 작동 [ ] 작동 안 함

### 6. 권한/보안
- AGENCY 역할만 접근: [ ] 확인됨 [ ] 확인 안 됨
- JWT에서 agencyId 바인딩: [ ] 확인됨 [ ] 확인 안 됨
- 스푸핑 차단: [ ] 확인됨 [ ] 확인 안 됨

### 7. 실패 원인 추정
1순위: [ ] placeName 정규화 미스매치
2순위: [ ] 저장 시점 누락
3순위: [ ] 스코프 누락
4순위: [ ] 디바운스 미적용/과도
5순위: [ ] 빈 목록 처리

### 8. 조치
- [ ] 프론트/백엔드 정규화 규칙 일치 확인
- [ ] 저장 시점 확인 (Network 캡처)
- [ ] 스코프 확인 (agencyId 필터링)
- [ ] 디바운스 적용 확인
- [ ] 빈 목록 처리 확인
- [ ] UI 개선 적용
```






