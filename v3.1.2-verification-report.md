# v3.1.2 충전 관리 검증 보고서

**작성일**: 2025년 11월 6일  
**대상 버전**: v3.1.2 (충전하기 제출 실패 트리아지 & 핫픽스)  
**검증 범위**: 프론트·백엔드 코드 정합성, CORS 설정, UI 동작 규칙

---

## 1. 검증 요약

### 1.1 코드 검증 결과

| 항목 | 상태 | 비고 |
|------|------|------|
| 프론트엔드 코드 | ✅ 적합 | page.tsx, lib/api.ts, types.ts, utils.ts 모두 지시사항 준수 |
| 백엔드 엔드포인트 | ✅ 적합 | AgencyBillingController 금액 검증(10K~5M), Idempotency 지원 |
| CORS 설정 | ⚠️ 보완 필요 → ✅ 완료 | Idempotency-Key 헤더 추가 필요 (핫픽스 적용) |
| 토스트 메시지 | ⚠️ 보완 필요 → ✅ 완료 | 서버 메시지 원문 표시 로직 추가 (핫픽스 적용) |
| 서버 기동 | ✅ 정상 | API(3001), Web(3000) 모두 Ready |

### 1.2 적용된 핫픽스

#### 핫픽스 #1: CORS allowedHeaders에 Idempotency-Key 추가
**파일**: `apps/api/src/main.ts`  
**변경 내용**:
```typescript
allowedHeaders: ['Authorization', 'Content-Type', 'Accept', 'Origin', 'X-Requested-With', 'Idempotency-Key'],
```
**사유**: 프론트엔드가 `POST /agency/topups`에서 `Idempotency-Key` 헤더를 전송하는데, 서버 CORS 설정에 해당 헤더가 없으면 preflight(OPTIONS) 단계에서 실패하여 실제 POST 요청이 도달하지 못함.

#### 핫픽스 #2: 성공 시 서버 메시지 원문 토스트 표시
**파일**: `apps/web/app/agency/billing/page.tsx`  
**변경 내용**:
```typescript
const res = await apiClient.createTopup(parsed, memo || undefined, key);
const serverMsg = (res as any)?.data?.message;
toast.success(serverMsg || '충전 요청이 접수되었습니다.');
```
**사유**: 지시사항 C) "서버 메시지 원문을 top-center로 정확히 표기" 준수. 기존 PENDING 재사용 시에도 서버가 반환하는 메시지를 그대로 사용자에게 표시.

---

## 2. Cursor 지시사항 준수 여부

### A) DevTools Network 캡처 (필수)
**상태**: ⚠️ 수동 테스트 필요  
**안내**: 사용자가 브라우저에서 다음 절차로 검증:
1. http://localhost:3000 접속 → agency1@test.com / agency123 로그인
2. /agency/billing 페이지 이동
3. DevTools Network 탭 열기
4. "충전하기" 클릭 → 금액 50,000원 입력 → 제출
5. **확인 항목**:
   - OPTIONS /agency/topups → 204 (Idempotency-Key allowed)
   - POST /agency/topups → 200 또는 201
   - Response Body에 `{ id, amount: 50000, status: "PENDING", memo }` 포함
   - Headers에 `Authorization: Bearer ...`, `Idempotency-Key: <uuid>` 존재

### B) 6가지 흔한 원인 점검

| 원인 | 서버 검증 | 프론트 검증 | 비고 |
|------|----------|------------|------|
| 1. 금액 범위/형식 위반 → 400 | ✅ | ✅ | 백엔드: `amount < 10000 \|\| amount > 5000000` 시 400 반환<br>프론트: `parseKRWInput`으로 천단위 콤마 제거 후 정수 전달 |
| 2. Idempotency 우회(기존 PENDING 재사용) | ✅ | ✅ | 서비스: `findOne({ userId, amount, status: 'PENDING', memo })`로 기존 요청 재사용<br>프론트: 200 수신 시 "성공 토스트 + 목록 새로고침" 동일 처리 |
| 3. 권한/토큰 문제 → 401/403 | ✅ | ✅ | JwtAuthGuard + RolesGuard(AGENCY) 적용<br>프론트: 401/403 시 "로그인 필요" 토스트 + `/` 리다이렉트 |
| 4. 429 속도 제한 | ⚠️ 미구현 | ✅ | 백엔드에 rate-limit 미들웨어 없음(권장사항에 기재)<br>프론트: 429 수신 시 3초 쿨다운 + "너무 잦은 요청" 토스트 |
| 5. CORS preflight 실패 | ✅ 수정됨 | - | Idempotency-Key 헤더 추가로 해결 |
| 6. 서버 500 | ✅ | ✅ | 에러 인터셉터로 `message` 추출 후 토스트 표시 |

### C) 프런트 제출 규칙 확인

| 규칙 | 구현 여부 | 코드 위치 |
|------|----------|----------|
| 버튼 클릭 시 로딩/disabled + Idempotency-Key | ✅ | `submitBusy` 상태 + `crypto.randomUUID()` |
| 성공 시 지갑/요청목록/거래원장 동시 재조회 | ✅ | `await loadAll()` 호출 |
| top-center 토스트 (서버 메시지 원문) | ✅ | `toast.success(serverMsg \|\| '...')` |
| PENDING만 취소 허용 | ✅ | `r.status === 'PENDING' && <button>` |

### 합격 기준 체크

| 기준 | 자동 검증 | 수동 검증 필요 | 결과 |
|------|----------|---------------|------|
| ✅ POST /agency/topups 2xx + 성공 토스트 | - | ✓ | 브라우저 테스트 필요 |
| ✅ 요청 목록에 PENDING 1건 즉시 보임 | - | ✓ | 브라우저 테스트 필요 |
| ✅ CSV/원장에 TOPUP_REQUEST 1줄 추가 | - | ✓ | 브라우저 테스트 필요 |
| ✅ 429/400/409/401/403/500 시 서버 메시지 표기 | ✅ | ✓ | 코드 로직 확인 완료, 실제 재현 권장 |

---

## 3. v3.2/v3.3 연동 고려사항

### v3.2 지갑 ↔ 원고 플로우 연동

**현재 상태**:
- BillingService에 `reserve`, `capture`, `release` 메서드 **이미 구현됨**
- Order 엔티티에 `reservedAmount`, `reservedAt` 필드 추가 필요 (마이그레이션)
- 환경변수 `UNIT_PRICE_PER_ORDER` 미설정 (기본값 1원으로 하드코딩)

**권장 작업**:
1. `Order` 엔티티에 다음 필드 추가:
   ```typescript
   @Column('int', { nullable: true })
   reservedAmount?: number;
   
   @Column({ type: 'timestamp', nullable: true })
   reservedAt?: Date;
   ```
2. AdminController의 `generateManuscript` 메서드에 예약 로직 삽입:
   ```typescript
   await this.billingService.reserve(orderId, UNIT_PRICE * submitCount);
   order.reservedAmount = UNIT_PRICE * submitCount;
   order.reservedAt = new Date();
   ```
3. AgencyController의 `reviewOrder(APPROVE)` 메서드에 차감 로직:
   ```typescript
   await this.billingService.capture(orderId);
   order.completedAt = new Date();
   ```
4. 취소/실패 시 `release` 호출

### v3.3 대행사 작성 중단(취소)

**현재 상태**:
- OrderStatus에 `CANCELED` 열거값 **이미 존재** (types.ts)
- AdminController에 `cancelOrder` 엔드포인트 **이미 구현됨**
- 대행사 측 취소 UI 미구현

**권장 작업**:
1. `OrderStatus`에 `CANCEL_REQUESTED` 추가
2. AgencyController에 `POST /agency/orders/:id/cancel` 엔드포인트 추가:
   - SUBMITTED/ADMIN_INTAKE → 즉시 CANCELED
   - GENERATING 이후 → CANCEL_REQUESTED
3. `/agency/orders/[id]/page.tsx`에 "작성 중단" 버튼 + 사유 모달 추가

---

## 4. 서버 기동 로그 (검증 시점)

### API 서버 (port 3001)
```
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/wallet, GET} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/transactions, GET} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/topups, GET} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/topups, POST} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/topups/:id, GET} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [RouterExplorer] Mapped {/agency/topups/:id/cancel, POST} route
[Nest] 26144  - 2025. 11. 06. 오후 9:18:00     LOG [Bootstrap] Application is running on: http://localhost:3001
```
**상태**: ✅ 정상 (모든 빌링 엔드포인트 매핑 완료)

### Web 서버 (port 3000)
```
▲ Next.js 14.2.33
- Local:        http://localhost:3000
- Environments: .env.local

✓ Starting...
✓ Ready in 1753ms
```
**상태**: ✅ 정상

### Docker 의존 서비스
```
NAMES     STATUS
pg16      Up 3 hours
redis7    Up 3 hours
```
**상태**: ✅ 정상

---

## 5. 트리아지 결과 및 조치 내역

### 문제 발견
1. **CORS preflight 실패 위험**: Idempotency-Key 헤더가 allowedHeaders 목록에 없음
2. **토스트 메시지 불일치**: 서버 메시지 원문 표시 로직 누락

### 원인 분석
1. 프론트엔드는 Cursor 지시사항대로 Idempotency-Key를 전송하지만, 백엔드 CORS 설정이 해당 커스텀 헤더를 허용하지 않아 브라우저가 preflight에서 차단
2. 성공 응답 시 기본 메시지만 표시하고, 서버가 반환하는 세부 메시지(예: "기존 PENDING 요청 재사용")를 무시

### 조치 사항
1. `apps/api/src/main.ts`의 CORS allowedHeaders에 `'Idempotency-Key'` 추가
2. `apps/web/app/agency/billing/page.tsx`의 createTopup 성공 핸들러에 `serverMsg` 추출 로직 추가

### 재현 불가 사항
- PowerShell 환경에서 curl 명령이 Invoke-WebRequest로 aliasing되어 직접 API 호출 테스트 실패
- **대안**: 브라우저 기반 E2E 테스트를 사용자가 직접 수행하도록 권장 (아래 시나리오 제공)

---

## 6. E2E 테스트 시나리오 (브라우저)

### 시나리오 1: 정상 충전 요청
1. http://localhost:3000 접속
2. agency1@test.com / agency123 로그인
3. 좌측 메뉴 "충전 관리" 클릭
4. "충전하기" 버튼 클릭
5. 금액: 50,000 입력
6. 메모: "테스트 충전" 입력
7. "제출" 클릭

**기대 결과**:
- ✅ 성공 토스트 표시 (서버 메시지 원문)
- ✅ 모달 닫힘
- ✅ 충전 요청 목록에 PENDING 1건 즉시 표시
- ✅ 거래 원장에 TOPUP_REQUEST 1줄 추가

### 시나리오 2: 금액 범위 위반 (400)
1. "충전하기" 버튼 클릭
2. 금액: 5,000 입력 (최소 10,000 미만)
3. "제출" 클릭

**기대 결과**:
- ✅ 에러 토스트: "충전 금액은 10,000~5,000,000원입니다" (서버 메시지)
- ✅ 모달 유지 (목록 갱신 없음)

### 시나리오 3: 중복 요청 (Idempotency)
1. 동일 금액(50,000) + 동일 메모("테스트 충전")로 다시 제출
2. 서버가 기존 PENDING 요청 재사용

**기대 결과**:
- ✅ 성공 토스트 표시 (서버가 "기존 요청 재사용" 메시지 반환 시 해당 메시지 표시)
- ✅ 목록에 새 건 추가 없음 (기존 건 유지)

### 시나리오 4: 취소
1. 목록에서 PENDING 상태 요청의 "취소" 버튼 클릭

**기대 결과**:
- ✅ 성공 토스트: "충전 요청이 취소되었습니다."
- ✅ 해당 건의 상태가 CANCELED로 변경
- ✅ 지갑 카드 및 거래 원장 갱신

### 시나리오 5: CSV 내보내기
1. 거래 원장에서 "CSV 내보내기" 버튼 클릭

**기대 결과**:
- ✅ `transactions_YYYY-MM-DD.csv` 파일 다운로드
- ✅ 헤더: 일시, 유형, 금액, 메모/참조, 상태, 요청ID, 주문ID, 거래ID
- ✅ TOPUP_REQUEST 레코드 포함

---

## 7. 권장 후속 작업

### 우선순위 높음
1. **속도 제한(Rate Limiting)**: 분당 5회 제한 미들웨어 추가 (현재 프론트에만 쿨다운 적용)
   ```typescript
   // apps/api/src/billing/billing-agency.controller.ts
   import { Throttle } from '@nestjs/throttler';
   
   @Throttle({ default: { limit: 5, ttl: 60000 } })
   @Post('topups')
   ```

2. **Idempotency 서버 측 강화**:
   - TopupRequest 엔티티에 `idempotencyKey` 컬럼 추가 (UNIQUE 제약)
   - 24시간 TTL 윈도우 적용

3. **v3.2 지갑 연동**: Order 엔티티 마이그레이션 + 산출/승인/취소 플로우 통합

### 우선순위 중간
4. **v3.3 대행사 취소 UI**: `/agency/orders/[id]` 페이지에 "작성 중단" 버튼 추가
5. **관리자 승인 플로우**: TopupRequest PENDING → APPROVED 전환 엔드포인트 (`POST /admin/topups/:id/approve`)

### 우선순위 낮음
6. **테스트 자동화**: Jest/Playwright로 E2E 시나리오 자동화
7. **로깅 강화**: 충전 요청/취소 시 감사 로그 추가

---

## 8. 결론

### 검증 완료 항목
- ✅ 프론트·백엔드 코드가 Cursor v3.1.2 지시사항을 준수함
- ✅ CORS 설정 핫픽스 적용 (Idempotency-Key 허용)
- ✅ 서버 메시지 원문 토스트 표시 로직 추가
- ✅ API/Web 서버 정상 기동 (3001/3000)
- ✅ 모든 빌링 엔드포인트 라우팅 확인

### 수동 검증 필요 항목
- ⚠️ 브라우저 기반 E2E 테스트 (위 시나리오 1~5)
- ⚠️ DevTools Network 탭으로 실제 HTTP 요청/응답 캡처
- ⚠️ 429 케이스 재현 (5회 연속 요청)

### 최종 상태
**코드 품질**: ✅ 합격  
**서버 가용성**: ✅ 정상  
**E2E 검증**: ⚠️ 사용자 수동 테스트 필요  
**v3.2/v3.3 준비도**: 🟡 부분 완료 (빌링 서비스 메서드 존재, UI/엔티티 작업 필요)

---

**검증자**: GitHub Copilot  
**검증 방법**: 정적 코드 분석 + 서버 기동 확인 + 핫픽스 적용  
**다음 단계**: 사용자가 브라우저에서 위 시나리오를 실행하고 캡처 결과를 GPT에게 전달
