# v2.5 (드로어 동기화 + 통과/반려 카운트) - 빠른 참조

## 🚨 즉시 확인 (5분)

### 1. 사전 체크리스트
- [ ] 대행사 권한 토큰(AGENCY)로 `/agency/**` 호출 OK
- [ ] 드로어 진입 시 `id` 쿼리 동기화 (`?id=`) 유지
- [ ] 대시보드 통계 API (`GET /agency/stats`)가 최신 버킷 규칙으로 응답 OK

### 2. 드로어 "상태 동기화" 규칙
- [ ] 열릴 때: 드로어 오픈 트리거 → 즉시 상세 재조회
- [ ] 유지: 리포커스/가시성 회복 시 1회 재조회, 10초 폴링, PASS/REJECT 성공 직후 강제 재조회
- [ ] 닫힐 때: 드로어 닫힘 → URL `?id=` 제거, 포커스 복원

### 3. 검수 액션 API 규칙
- [ ] 승인: `POST /agency/orders/:id/review` Body: `{ decision: "APPROVE" }`, 트랜잭션 처리
- [ ] 반려: `POST /agency/orders/:id/review` Body: `{ decision: "REJECT", reason: "..." }`, 트랜잭션 처리
- [ ] 검증/제한: 반려 사유 필수, 길이 300자 이내, Idempotency-Key, 409 처리

### 4. 드로어 UI 동기화
- [ ] 오픈 시: 스켈레톤 → `GET /agency/orders/:id` 2xx → 본문/카운트/리포트 렌더
- [ ] 버튼/카운트 업데이트: optimistic 업데이트, 서버 성공/실패 처리
- [ ] 리스트/상단 카드 동기화: 액션 성공 시 항목 제거/이동, 상단 카드 즉시 재조회

### 5. 레이스/중복 처리
- [ ] 동일 주문에 대한 승인/반려 요청은 버튼 잠금 + Idempotency-Key로 중복 방지
- [ ] 상태 불일치 → 409 수신 시 드로어 전체 재조회 후 안내 토스트

### 6. 권한/보안
- [ ] 엔드포인트는 AGENCY 전용, JWT에서 `agencyId` 바인딩
- [ ] 다른 대행사 주문 접근 시 403
- [ ] 서버 로깅: 모든 필드 확인

### 7. 관측/장애 복구
- [ ] 큐/워커 상태 확인: `/health`의 `queue.waiting|active|failed`
- [ ] 반려 후 자동 재생성 실패 시: "재생성 실패" 배지 표시 + "재시도" 버튼

---

## ✅ 합격 기준

1. [ ] 드로어 열릴 때, 상세/카운트/리포트가 항상 최신 (오픈/리포커스/액션 후 재조회)
2. [ ] 승인: 상태가 `COMPLETE`로 바뀌고 `approveCount` 증가, 상단 카드 (금일 완료) 즉시 반영
3. [ ] 반려: 상태가 `REGEN_QUEUED`로 바뀌고 `rejectCount` 증가, 리스트 항목이 "작성 중" 버킷으로 이동
4. [ ] 409/4xx 시 정중한 토스트 + 드로어 재조회로 정합 회복
5. [ ] 모든 알림은 top-center로 서버 메시지 원문 표시

---

## 🔧 조치 요약

### 드로어 "상태 동기화" 규칙
- 열릴 때: 드로어 오픈 트리거 → 즉시 상세 재조회
- 유지: 리포커스/가시성 회복 시 1회 재조회, 10초 폴링, PASS/REJECT 성공 직후 강제 재조회
- 닫힐 때: 드로어 닫힘 → URL `?id=` 제거, 포커스 복원

### 검수 액션 API 규칙
- 승인: `POST /agency/orders/:id/review` Body: `{ decision: "APPROVE" }`, 트랜잭션 처리
- 반려: `POST /agency/orders/:id/review` Body: `{ decision: "REJECT", reason: "..." }`, 트랜잭션 처리
- 검증/제한: 반려 사유 필수, 길이 300자 이내, Idempotency-Key, 409 처리

### 드로어 UI 동기화
- 오픈 시: 스켈레톤 → `GET /agency/orders/:id` 2xx → 본문/카운트/리포트 렌더
- 버튼/카운트 업데이트: optimistic 업데이트, 서버 성공/실패 처리
- 리스트/상단 카드 동기화: 액션 성공 시 항목 제거/이동, 상단 카드 즉시 재조회

### 레이스/중복 처리
- 동일 주문에 대한 승인/반려 요청은 버튼 잠금 + Idempotency-Key로 중복 방지
- 상태 불일치 → 409 수신 시 드로어 전체 재조회 후 안내 토스트

### 권한/보안
- 엔드포인트는 AGENCY 전용, JWT에서 `agencyId` 바인딩
- 다른 대행사 주문 접근 시 403
- 서버 로깅: 모든 필드 확인

### 관측/장애 복구
- 큐/워커 상태 확인: `/health`의 `queue.waiting|active|failed`
- 반려 후 자동 재생성 실패 시: "재생성 실패" 배지 표시 + "재시도" 버튼






