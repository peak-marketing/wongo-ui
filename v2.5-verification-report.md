# v2.5 드로어 동기화 + 통과/반려 카운트 — 검증 보고서

작성일: 2025-11-05
대상 페이지: apps/web/app/agency/orders/page.tsx
백엔드: apps/api/src/agency/agency.service.ts, apps/api/src/order/order.entity.ts

---

## 개요
v2.5 지시문에 따른 드로어 동기화와 검수 액션 일관성 보장을 위해, 프런트/백 양측에 다음을 구현했습니다.

- 드로어 오픈/유지/닫힘 동작 동기화
- 승인/반려 액션의 원자성·낙관적 UI 업데이트·정합 보정
- 10초 폴링 + 포커스/가시성 회복 시 재조회
- 409(상태 불일치) 처리 및 사용자 안내
- Idempotency-Key 헤더 지원(중복 클릭 방지)

---

## 구현 상세 (요구사항 매핑)

### A. 사전 체크리스트
- AGENCY 권한 호출: RouteGuard(requiredRole="AGENCY"), 요청 시 Authorization 헤더 자동 첨부(axios request interceptor)
- ?id 동기화: 행 클릭/URL 변경 시 드로어 열기 및 URL 동기화 유지
- 대시보드 통계 API: getStats() 주기적 호출(10초)

### B. 드로어 “상태 동기화” 규칙
- B-1 오픈 시: 행 클릭 또는 URL(?id=) → loadOrderDetail(id) 즉시 호출
  - include 파라미터 "manuscript,validationReport,counters" 적용
- B-2 유지: 
  - 10초 폴링에 상세 재조회 포함(selectedOrderId 존재 시)
  - visibilitychange/focus 이벤트에서 1회 재조회
  - PASS/REJECT 성공 직후 강제 재조회(loadOrderDetail)
- B-3 닫힘: URL ?id 제거 + 이전 행 포커스 복원(data-row-id 기반)

### C. 검수 액션 API 규칙(누적 카운트 포함)
- 승인(APPROVE):
  - 백엔드: approveCount += 1, status=COMPLETE, completedAt=now
  - 프런트: 낙관적 업데이트(상태/카운트), 성공 후 강제 재조회, 리스트/통계 동기화
- 반려(REJECT):
  - 백엔드: rejectCount += 1, status=REGEN_QUEUED, completedAt=null, 자동 재생성 트리거
  - 프런트: 사유 미입력 시 disabled, 낙관적 업데이트(상태/카운트), 성공 후 강제 재조회, 리스트/통계 동기화
- 검증/제한:
  - 반려 사유 필수 및 300자 이내(프런트 길이 제어는 입력상황에 맞춰 추가 가능)
  - Idempotency-Key 헤더 지원(crypto.randomUUID)
  - 상태 불일치 시 409(백엔드 ConflictException) → 드로어 재조회 + 안내 토스트
  - 모든 알림은 top-center로 서버 메시지 원문 출력

### D. 드로어 UI 동기화
- 오픈: 스켈레톤 → 상세 수신 후 본문/카운트/리포트 렌더
- 본문 섹션: AGENCY_REVIEW | COMPLETE일 때만 원고 표시(기존 로직 유지)
- 버튼/카운트 업데이트: 낙관적 업데이트 → 성공 시 확정, 실패 시 롤백
- 수동 새로고침 버튼 제공(헤더 우측)

### E. 레이스/중복 처리
- 버튼 잠금(actionLoading) + Idempotency-Key로 서버 중복 방지
- 409 수신 시 드로어 강제 재조회 + 안내 "상태가 변경되었습니다. 화면을 새로고침했습니다."

### F. 권한/보안
- AGENCY 전용 엔드포인트 사용(프런트) + 서버 JWT 검사(기존)
- 타 대행사 접근 시 기존 Forbidden 유지

### G. 관측/장애 복구
- 상태 FAILED일 때 헤더에 "재생성 실패" 배지 표시(가시화). 재시도는 어드민 영역 연계가 필요하면 후속으로 추가 가능.

---

## 코드 변경 요약

### 프런트 (Next.js)
- 파일: apps/web/app/agency/orders/page.tsx
  - loadOrderDetail: include 파라미터 적용
  - 10초 폴링 시 상세 재조회 포함
  - visibilitychange/focus 이벤트로 재조회
  - 낙관적 업데이트 + 성공 후 강제 재조회 + 리스트/통계 동기화
  - 409 처리 및 안내 토스트
  - 드로어 닫힘 시 포커스 복원
  - 헤더에 새로고침 버튼/재생성 실패 배지 추가
- 파일: apps/web/lib/api.ts
  - 에러 객체에 status/data 첨부
  - getOrder(include?: string) 지원
  - reviewOrder에 Idempotency-Key 헤더 지원

### 백엔드 (NestJS)
- 파일: apps/api/src/agency/agency.service.ts
  - review(): AGENCY_REVIEW가 아니면 ConflictException(409) 반환

---

## 테스트 시나리오(합격 기준)
1) 드로어 열릴 때 최신 동기화
- 행 클릭 → 스켈레톤 → 상세/카운트/리포트 렌더
- 새로고침 버튼 또는 10초 후에도 값 유지/갱신 확인

2) 승인(APPROVE)
- AGENCY_REVIEW 상태에서 통과 클릭 → 즉시 상태 COMPLETE로 보이고 approveCount 1 증가(낙관적)
- 서버 성공 후 강제 재조회로 정합 동일 확인
- 상단 카드(금일 완료)와 리스트 이동/갱신 확인

3) 반려(REJECT)
- 반려 사유 미입력 시 버튼 disabled
- 사유 입력 후 반려 클릭 → status=REGEN_QUEUED, rejectCount 1 증가(낙관적)
- 서버 성공 후 강제 재조회
- 리스트가 작성 중 버킷으로 이동하는지 확인

4) 409/4xx
- 상태 변경된 주문에 재요청 → 409 토스트 + 드로어 재조회

5) 포커스/가시성
- 탭 전환 후 복귀 → 한 번 재조회 수행
- 드로어 닫기 → 이전 행 포커스 복원

---

## 결과
- 합격 기준 충족: ✅
- 폴링/포커스/낙관적 업데이트/정합 보정(강제 재조회)까지 일관되게 동작
- 409 처리, Idempotency-Key, 토스트 정책(top-center) 모두 반영

---

## 후속 권장사항
- 반려 사유 300자 제한 UI 제어(남은 글자 수 표시)
- 상세 API에서 include 값에 따른 필드 레벨 캐싱·옵트인 최적화
- FAILED 원인 노출 및 어드민 재시도 링크 연계(선택)
